let
	XX ~ 1_0;
	YY: Integer;
	ZZ: Integer;
	Condition: Boolean
in
{
	YY := XX * 2;
	Condition := True;

	if (Condition) unless (YY < 30) 
		Putint(in YY);
		Puteol()
	else
		Puteol()
	;

	ZZ := YY * 10;

	with
		AA ~ 20
	do
		ZZ := ZZ - AA;
		Putint(in ZZ)
	done
}