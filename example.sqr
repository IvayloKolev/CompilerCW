let
	XX ~ 1_0;
	YY: Integer;
	Condition: Boolean
in
{
	YY := XX * 2;
	Condition := True;

	if (Condition) unless (YY < 30) 
		YY := YY + 69;
		YY := YY * 69;
		Putint(in YY);
		Puteol()
	else
		Puteol()
	;
}